# StudyApp - AI-Powered Learning Platform

A comprehensive web application for educational management with AI-powered features, enabling teachers to upload study materials and students to access personalized learning roadmaps.

## 🚀 Features

### For Students
- 📚 **Browse & Download Notes** - Access study materials uploaded by teachers
- 🗺️ **AI-Powered Study Roadmap** - Get personalized learning paths generated by Gemini AI
- 📊 **Progress Tracking** - Monitor your learning journey
- 🤖 **AI Study Assistant** - Get instant help and recommendations

### For Teachers
- 📤 **Upload Notes** - Share study materials with students
- 👥 **Student Management** - View and track student progress
- 📈 **Analytics Dashboard** - Monitor class performance

### General Features
- 🔐 **Role-Based Authentication** - Separate interfaces for teachers and students
- 🎨 **Modern UI/UX** - Premium design with dark mode support
- 📱 **Responsive Design** - Works seamlessly on all devices
- ⚡ **Real-time Updates** - Powered by Firebase

## 🛠️ Tech Stack

- **Frontend**: React + Vite
- **Styling**: Vanilla CSS with modern design system
- **Backend**: Firebase (Auth, Firestore, Storage)
- **AI**: Google Gemini AI
- **Routing**: React Router DOM

## 📋 Prerequisites

Before you begin, ensure you have:
- Node.js (v16 or higher)
- npm or yarn
- Firebase project
- Google Gemini AI API key

## 🔧 Setup Instructions

### 1. Clone and Install

```bash
cd StudyApp
npm install
```

### 2. Configure Firebase

1. Go to [Firebase Console](https://console.firebase.google.com/)
2. Create a new project or use an existing one
3. Enable the following services:
   - **Authentication** (Email/Password)
   - **Firestore Database**
   - **Storage**
4. Get your Firebase configuration from Project Settings

### 3. Get Gemini AI API Key

1. Go to [Google AI Studio](https://makersuite.google.com/app/apikey)
2. Create a new API key
3. Copy the key

### 4. Configure Environment Variables

Update the `.env` file in the root directory with your credentials:

```env
# Firebase Configuration
VITE_FIREBASE_API_KEY=your_firebase_api_key_here
VITE_FIREBASE_AUTH_DOMAIN=your_project_id.firebaseapp.com
VITE_FIREBASE_PROJECT_ID=your_project_id
VITE_FIREBASE_STORAGE_BUCKET=your_project_id.appspot.com
VITE_FIREBASE_MESSAGING_SENDER_ID=your_messaging_sender_id
VITE_FIREBASE_APP_ID=your_app_id

# Gemini AI Configuration
VITE_GEMINI_API_KEY=your_gemini_api_key_here
```

### 5. Set Up Firestore Security Rules

In Firebase Console, go to Firestore Database > Rules and add:

```javascript
rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Users collection
    match /users/{userId} {
      allow read: if request.auth != null;
      allow write: if request.auth != null && request.auth.uid == userId;
    }
    
    // Notes collection
    match /notes/{noteId} {
      allow read: if request.auth != null;
      allow create: if request.auth != null && request.resource.data.uploadedBy == request.auth.uid;
      allow update, delete: if request.auth != null && resource.data.uploadedBy == request.auth.uid;
    }
    
    // Progress collection
    match /progress/{userId} {
      allow read, write: if request.auth != null && request.auth.uid == userId;
    }
  }
}
```

### 6. Set Up Storage Security Rules

In Firebase Console, go to Storage > Rules and add:

```javascript
rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    match /notes/{allPaths=**} {
      allow read: if request.auth != null;
      allow write: if request.auth != null;
    }
  }
}
```

## 🚀 Running the App

### Development Mode

```bash
npm run dev
```

The app will be available at `http://localhost:5173`

### Production Build

```bash
npm run build
npm run preview
```

## 📱 Usage Guide

### For Students

1. **Sign Up**: Create an account and select "Student" role
2. **Browse Notes**: Navigate to the Notes section to view available materials
3. **Generate Roadmap**: Go to Study Roadmap and select your topic and level
4. **Track Progress**: Monitor your learning journey on the dashboard

### For Teachers

1. **Sign Up**: Create an account and select "Teacher" role
2. **Upload Notes**: Use the Upload Notes page to share materials
3. **View Students**: Check student progress and analytics
4. **Manage Content**: Edit or delete your uploaded notes

## 🎨 Design Features

- **Modern Color Palette**: Vibrant gradients and harmonious colors
- **Glassmorphism**: Beautiful glass-like UI elements
- **Smooth Animations**: Micro-interactions for better UX
- **Dark Mode**: Automatic theme support
- **Responsive**: Mobile-first design approach

## 📂 Project Structure

```
StudyApp/
├── src/
│   ├── components/       # Reusable components
│   │   ├── Navbar.jsx
│   │   └── ProtectedRoute.jsx
│   ├── config/          # Configuration files
│   │   ├── firebase.config.js
│   │   └── gemini.config.js
│   ├── context/         # React context providers
│   │   └── AuthContext.jsx
│   ├── pages/           # Page components
│   │   ├── HomePage.jsx
│   │   ├── Login.jsx
│   │   ├── Signup.jsx
│   │   ├── TeacherDashboard.jsx
│   │   ├── NotesUpload.jsx
│   │   ├── StudentDashboard.jsx
│   │   └── StudyRoadmap.jsx
│   ├── services/        # API services
│   │   ├── firebaseService.js
│   │   └── geminiService.js
│   ├── App.jsx          # Main app component
│   ├── main.jsx         # Entry point
│   └── index.css        # Global styles
├── .env                 # Environment variables
├── .env.example         # Environment template
└── package.json
```

## 🔑 Key Features Explained

### AI-Powered Roadmap
The app uses Google's Gemini AI to generate personalized learning paths based on:
- Selected topic (Web Development, Data Science, etc.)
- Skill level (Beginner, Intermediate, Advanced)
- Learning goals and preferences

### Notes Management
- Teachers can upload files (PDF, DOC, PPT, etc.)
- Files are stored securely in Firebase Storage
- Students can browse, search, and download materials
- Category-based organization

### Progress Tracking
- Real-time progress updates
- AI-generated insights and recommendations
- Visual analytics and charts

## 🐛 Troubleshooting

### Blank Page Issue
If you see a blank page:
1. Check browser console for errors
2. Ensure Firebase credentials are correctly configured in `.env`
3. Verify Gemini AI API key is valid
4. Clear browser cache and reload

### Firebase Errors
- Ensure all Firebase services are enabled
- Check security rules are properly configured
- Verify API keys and project IDs are correct

### Build Errors
```bash
# Clear node_modules and reinstall
rm -rf node_modules package-lock.json
npm install
```

## 📝 Environment Variables

The app requires the following environment variables:

| Variable | Description | Required |
|----------|-------------|----------|
| `VITE_FIREBASE_API_KEY` | Firebase API key | Yes |
| `VITE_FIREBASE_AUTH_DOMAIN` | Firebase auth domain | Yes |
| `VITE_FIREBASE_PROJECT_ID` | Firebase project ID | Yes |
| `VITE_FIREBASE_STORAGE_BUCKET` | Firebase storage bucket | Yes |
| `VITE_FIREBASE_MESSAGING_SENDER_ID` | Firebase messaging sender ID | Yes |
| `VITE_FIREBASE_APP_ID` | Firebase app ID | Yes |
| `VITE_GEMINI_API_KEY` | Google Gemini AI API key | Yes |

## 🤝 Contributing

Contributions are welcome! Please feel free to submit a Pull Request.

## 📄 License

This project is open source and available under the MIT License.

## 🙏 Acknowledgments

- Google Gemini AI for AI-powered features
- Firebase for backend services
- React and Vite for the frontend framework

## 📞 Support

If you encounter any issues or have questions, please:
1. Check the troubleshooting section
2. Review Firebase and Gemini AI documentation
3. Open an issue on the repository

---

**Built with ❤️ using React, Firebase, and Gemini AI**
